#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,x,sum=0;
    cin>>n;
    while(n--){
        cin>>x;
        sum+=x;
    }
    if(sum>0)
        cout<<"HARD\n";
    else
        cout<<"EASY\n";

    return 0;
}
