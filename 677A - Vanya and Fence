#include <iostream>

using namespace std;

int main()
{
    long long x,y,sum=0,a[x+1];
    cin>>x>>y;
    for(int i=0;i<x;i++){
        cin>>a[i];
        if(a[i]>y)
            sum+=2;
        else
            sum++;
    }


    cout<<sum<<endl;
    return 0;
}
